# LeaveBy

Simple prototype for MOR-531: a helper that looks at calendar events and tells you **when to leave**.

This version uses a **sample Google Calendar day** (Connect Google Calendar loads mock events). It does not use a real Google login.

```bash
npm install
npm run dev
```
